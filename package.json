{
  "name": "personal-video-digest",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "bin": {
    "video-digest": "./bin/video-digest"
  },
  "scripts": {
    "test": "bun test --pass-with-no-tests",
    "typecheck": "tsc --noEmit",
    "video-digest": "bun --silent run src/cli/main.ts",
    "web": "bun run src/web/server.ts",
    "start": "bun run src/web/server.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^6.0.3"
  }
}
